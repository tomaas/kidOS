# dependencies
/node_modules

# TanStack Start / Nitro
/.tanstack/
/.output/
/.nitro/
/.vercel/

# build
/build

# misc
.DS_Store
*.pem

# machine-specific deploy overrides
/compose.override.yml
/local/
.claude/settings.local.json
.claude/worktrees/

# env
.env*
!.env.example

# generated stories, images, audio + local SQLite db
/data/*
!/data/.gitkeep

# db dumps taken at the repo root (family data — never publish)
/dump.sql

# cursive reading font — personal-use license, not redistributable
# (see public/fonts/README.md)
/public/fonts/cursive.woff
/public/fonts/LICENSE-cursive.txt

# typescript
*.tsbuildinfo
.gstack/

# Scratch DATA_DIR des worktrees agents (dev sur port alternatif)
data-dev/
